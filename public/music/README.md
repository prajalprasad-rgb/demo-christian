# Wedify Music Folder

Place background music here, for example:

- `wedding-music.mp3`
- `scratch-reveal.mp3`

Then update `backgroundMusic.src` and `scratchReveal.sound` in `src/data/weddingData.js`.
